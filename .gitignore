# --- ไฟล์ความลับ (ห้ามเอาขึ้นเด็ดขาด) ---
.env

# --- สภาพแวดล้อมจำลอง (Python Virtual Environment) ---
.venv/
venv/
env/

# --- ไฟล์ขยะของ Mac ---
.DS_Store

# --- ไฟล์แคชของ Python ---
__pycache__/
*.pyc
*.pyo

# --- ไฟล์ข้อมูลและโมเดลขนาดใหญ่ (ป้องกัน Repo เต็ม) ---
# (ถ้าไฟล์โมเดล .joblib ของคุณขนาดเล็กกว่า 50MB จะเอา # ข้างล่างออกเพื่อให้มันอัปโหลดโมเดลขึ้นไปด้วยก็ได้ครับ)
*.csv
*.joblib 

# --- ไฟล์ตั้งค่าของ VS Code ---
.vscode/